import { GamePlayData, InventoryList } from "../App";

interface Props {
  inventoryList: InventoryList;
  gamePlayData: GamePlayData;
  onShop: () => void;
  onTravel: () => void;
}

const Operations = () => {
  return <div>Operations</div>;
};

export default Operations;
